#include<iostream>
using namespace std;
int main()
{
    int n;
    cout<<"Enter the number : ";//only one IF condition is execute. those condition is right.
    cin>>n;
    if(n%2==0)
    cout<<"It is the even no.  ";
    if(n%2!=0);
    cout<<"It is odd no. ";
}